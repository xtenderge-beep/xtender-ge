# Текущее состояние Xtender

**Актуальные задачи и их статус — [tasks.md](tasks.md). Перед началом любой
новой работы сверяться с ним, после завершения — отмечать там же.** Этот
документ описывает архитектуру и историю, а не то, что делать дальше.

Снимок на **17 сентября 2026 года**. Проверенная версия приложения:
`21f5000` (см. `git log` для полного хэша), ветка `main`.
Документ описывает реализованный код и последнюю проверку запуска, а не
текущие численные показатели производственной базы.

## Запуск и подтверждённые проверки

- Коммиты `e26f423`…`b104bfa` (15.09), `0f3eb62`…`aed6937` (16.09, 12
  коммитов — регистрация/кабинет, модератор из Telegram, категории из
  админки, размер `van`, см. «Категории услуг и размер van» ниже) и
  `cd03f93`…`21f5000` (17.09, см. «Модерация и реферальные ссылки» ниже)
  отправлены в `origin/main`.
- Авторизованная проверка производственной админки в этой сессии не
  проводилась; Railway-деплой после push не отслеживался в реальном времени
  (нет доступа к панели Railway из этой сессии) — статус смотреть отдельно.
- На 15.09: полный набор из 15 файлов тестов (`tests/*.test.js`) и
  `npm run build` прошли локально на pg-mem; реальные SMS не отправлялись.
- На 16.09: полный набор `tests/*.test.js` и `npm run build` в той сессии
  НЕ прогонялись — каждая из 12 правок проверялась отдельным одноразовым
  pg-mem-скриптом (создавался, прогонялся, удалялся; см. описания в
  `tasks.md`), без покрытия в постоянном `tests/`.
- На 17.09: этот пробел закрыт — полный набор `tests/*.test.js` (19 файлов,
  включая один реально сломанный и починенный по ходу дня, см. «Модерация и
  реферальные ссылки») прогонялся после каждой правки и проходит целиком.
  `npm run build` в этой сессии не запускался.
- На 18.09 (закрытие по категориям и воронка по группам — коммиты `c843f74`,
  `d2fa1c4`, `aeff293`, `c576a8c`, см. «Заявка из нескольких категорий» ниже):
  полный набор `tests/*.test.js` (21 файл) проходит на pg-mem, новые проверки
  сразу оформлены постоянными тестами, а не одноразовыми скриптами.
  `npm run build:fa` запускался (обновлён `public/fa/`), `npm run build` — нет.
  Деплой Railway и вид боевой страницы после него не проверялись — см. задачу
  «Проверка на проде» в [tasks.md](tasks.md).
- Живой прогон на `node dev-server.js` (pg-mem): главная страница, вход
  мастера (`/api/master/login/request-code`), OTP-заявка — без ошибок
  (проверено 15.09). 17.09 тем же способом воспроизведён и исправлен реальный
  проблем на проде — `/join` отдавал 500 на всех локалях (см. ниже).

Railway разворачивает приложение после push в main. PostgreSQL и Redis —
отдельные сервисы проекта `fabulous-luck`; домен подключён через Cloudflare.
Успешный push сам по себе не подтверждает успешный деплой: нужно смотреть status.

## Инцидент 14–15.09.2026: краш-луп миграции на проде

Коммит `ae9a1c3` (тестовые заявки + только грузинские номера, см.
[technical-testing.md](technical-testing.md) и
[georgian-phone-numbers.md](georgian-phone-numbers.md)) добавил в `schema.sql`
view `business_orders`/`business_masters` (`SELECT * FROM orders/masters …`).
Их view-правило зафиксировало тип колонки `orders.token`, которую чуть выше
по тому же файлу на каждом старте безусловно приводит
`ALTER TABLE orders ALTER COLUMN token TYPE VARCHAR(20)` (старая миграция
«короткий токен для SMS»). Postgres не разрешает менять тип колонки, от
которой зависит view — первый деплой прошёл (view ещё не существовал в
момент ALTER), но каждый следующий рестарт контейнера падал на
`cannot alter type of a column used by a view or rule`, и Railway уходил в
бесконечный краш-луп (сайт был полностью недоступен, не только вход мастеров).

pg-mem не исполняет этот путь так же, как реальный Postgres (не проверяет
зависимости view при ALTER), поэтому весь набор тестов и локальный
`dev-server.js` проходили зелёными и на сломанной версии — баг был виден
только в логах Railway.

**Фикс (`e26f423`)**: `src/app.js` перед прогоном `schema.sql` дропает эти
два view (`DROP VIEW IF EXISTS business_orders, business_masters CASCADE`);
`schema.sql` пересоздаёт все пять view (включая три зависимых) в конце того
же прогона. Изменение точечное — 7 строк в одном файле, `schema.sql`,
`dev-server.js` и тесты не тронуты. Проверено: все 15 тестов и живой
`dev-server.js` проходят.

**Не исправлено, отдельная задача**: в том же коммите `ae9a1c3`
(`master.controller.js`, `loginRequestCode`/`loginVerify`) вход мастера
потерял нормализацию номера через `toE164()` перед поиском в БД — теперь
принимается только точный формат `+995XXXXXXXXX` без нормализации. Формы
сайта подставляют `+995` заранее, поэтому обычный ввод через сайт не задет;
риск — автозаполнение браузером/менеджером паролей старым форматом номера
или сторонние клиенты API. См. «Следующие потребности» ниже.

## Что работает

| Область | Состояние |
|---|---|
| Стек | Node.js, Express, EJS, PostgreSQL, Redis, Tailwind CSS |
| Языки | Грузинский, русский, английский; админка на русском |
| Клиентские заявки | Подтверждение телефона, модерация, рассылка по категориям, открытия и нажатия контактов, закрытие владельцем |
| Каталог | Отдельный платный показ номера после подтверждения телефона клиента |
| Исполнители | Регистрация, модерация, кабинет, баланс, пополнения, история списаний, поддержка, промокоды |
| Менеджеры | Текущее назначение исполнителям, промокоды, приглашения клиентов, карточки менеджеров |
| Платежи | Банковское пополнение с документами/чеками; админ подтверждает/отменяет любой счёт на `/admin/receipts` (не только с чеком), ищет по номеру/имени/телефону, сверяет банковскую выписку (.xlsx) с автосопоставлением, ручной привязкой нераспознанных платежей и дедупликацией — см. ниже; онлайн-оплаты картой нет |
| Сессии исполнителей | Отзываемые сессии в Redis; секретная ссылка сама по себе не заменяет вход |
| Администратор | Пароль, подписанная сессия, CSRF, ограничения попыток; SMS-2FA при соответствующей настройке |
| Согласия | Привязка согласий к действиям, журнал, архив документов, экспорт |
| Тестовые заявки | Списки технических номеров клиентов/исполнителей в `/admin/settings/technical`; параллельно с обычными заявками, без общей паузы рассылки — [technical-testing.md](technical-testing.md) |
| Подтверждение телефона | Только `+995` + 9 цифр во всех публичных сценариях (заявки, регистрация/вход исполнителей, отзывы, каталог) — [georgian-phone-numbers.md](georgian-phone-numbers.md) |

Текущие тарифы и суммы на счетах задаются данными и настройками приложения.
Этот снимок не фиксирует значения тарифов или число реальных пользователей.

## Партнёрская программа — релиз 13.09.2026

Подробное руководство: [manager-partners.md](manager-partners.md).

- Админка → **Менеджеры → карточка → Партнёрская программа**: постоянная
  реферальная ссылка специалиста и регулируемая ставка. Начальная ставка —
  **25%**, индивидуально меняется администратором в пределах 0–100%.
- Ссылка: `/join?ref=<token>`. Первый действительный переход сохраняется
  в httpOnly-cookie на 30 дней. Источник закрепляется при новой регистрации;
  повторная регистрация того же телефона не переносит его другому менеджеру.
- `referral_manager_id` — закреплённый источник для комиссии;
  `manager_id` — текущий ответственный. Переназначение ответственного
  не переносит право на комиссию автоматически.
- Комиссия начисляется с каждого положительного `topup`, включая повторные.
  Бонусы, ручные корректировки и списания исключены. Сумма и ставка сохраняются
  отдельно для каждой операции; изменение процента не пересчитывает историю.
- Начисление комиссии не уменьшает баланс специалиста. Пополнение и комиссия
  записываются атомарно; повторная обработка одной операции не дублирует комиссию.
- **Выплаты менеджерам** (`/admin/partner-payouts`): месяц по Тбилиси,
  пополнения, начислено, выплачено, остаток и старый долг; детализация до операции.
  Платежи относятся к месяцу начисления, фактическое время записи хранится отдельно.
- Выплата фиксируется после реального перевода. Поддержаны частичные выплаты,
  защита от превышения долга и отмена ошибочной записи с причиной.
  Ни запись, ни её отмена не выполняют банковскую операцию.
- Старые специалисты привязываются явно через карточку менеджера с основанием;
  только если источник ещё не закреплён. Начисления начинаются с будущих
  пополнений. Исторические пополнения автоматически не пересчитываются.
- Выключение менеджера запрещает новые приглашения, но сохраняет начисления
  по закреплённым специалистам. Остановка будущих начислений — ставка 0%.

Добавлены `partner.service.js`, `partner.controller.js`, администраторские
формы и `tests/partners.test.js`. Схема добавляет поля managers/masters,
таблицы manager_commissions, manager_payouts и partner_audit. Миграция
добавочная и идемпотентная, не делает выплат и не меняет старые балансы.

Пока не реализованы автоматические банковские переводы, кабинет менеджера
с доступом к этому отчёту, автоматическая корректировка комиссии при возврате
реальных денег и автоматический перенос права на комиссию другому менеджеру.

## Сверка пополнений и счетов исполнителей — 16.09.2026

`/admin/receipts` (`src/controllers/admin.controller.js`, `src/services/receipt.service.js`,
`src/services/bankStatement.service.js`, `src/views/admin/receipts.ejs`) переписан почти
полностью — владелец спросил, как администратор видит сформированные исполнителями счета
и подтверждает по ним оплату. Коммиты: `f0439fd`, `4042ea5`, `54f9200`, `b0a41d7`, `ae53048`,
`1e3145d`.

- **Список — по счетам, а не по чекам.** Раньше страница строилась от `topup_receipts`
  (загруженных файлов); счёт без чека был администратору не виден вовсе. Теперь
  `receiptService.list()` берёт все `topup_requests` (кроме `cancelled`) и подмешивает
  последний прикреплённый чек, если он есть, — счёт без чека тоже виден, с кнопкой
  подтверждения.
- **Одна кнопка — «Подтвердить оплату и зачислить».** `receiptService.confirmPayment()`
  — единственное место, которое реально зачисляет по счёту: идёт через
  `masterService.adjustBalance` (тот же путь, что и любое другое движение баланса),
  помечает счёт и его чек (если есть) `credited`. Комментарий обязателен, если введённая
  сумма отличается от запрошенной в счёте. Кнопка ускоряет уже принятое решение — она
  не заменяет проверку админом реального поступления в банке.
- **Отмена счёта** (`receiptService.cancel()`) — мягкий статус `cancelled`, не `DELETE`
  (есть FK на прикреплённый чек, и нужна история), исключается из списка. Нужно было,
  потому что исполнитель может создать несколько счетов и оплатить только один.
- **Поиск** по номеру счёта, имени и телефону (`?q=` на `/admin/receipts`).
- **Загрузка банковской выписки (.xlsx)** — `bankStatementService.parseCredits()`,
  библиотека `exceljs` (новая зависимость; тянет один устаревший транзитивный `uuid`
  с умеренной уязвимостью — не по достижимому нами коду, актуально для будущего аудита
  зависимостей). Формат — экспорт бизнес-выписки Bank of Georgia; строка заголовков
  ищется по колонке `Date` (не по номеру строки — над таблицей всегда несколько строк
  реквизитов). Из зачислений (`Credit In Lari`) вытаскивается подстрока вида
  `XT-XXXXXXXXXXXX` — номер счёта, генерируемый `topup.service.js`. **Важно (найдено на
  реальном файле владельца)**: в одной и той же строке колонки `Nomination` и
  `Additional Info` могут содержать РАЗНЫЕ номера (устаревший рядом с актуальным) —
  парсер ищет ссылку сразу во всех трёх текстовых колонках (`Nomination`,
  `Entry Comment`, `Additional Info`).
- **Совпавшие счета** поднимаются в начало списка; сумма в форме подтверждения
  подставляется РЕАЛЬНАЯ из выписки, а не запрошенная — на реальном примере владельца
  счёт был выставлен на 10 ₾, а пришло 5 ₾, комментарий о расхождении подставляется
  автоматически.
- **Нераспознанные платежи** (нет номера в назначении, или номер не найден среди
  счетов — люди ошибаются или стирают текст назначения) — отдельный список с суммой,
  датой и текстом назначения и выпадающим списком неоплаченных счетов для ручной
  привязки (`/admin/receipts/assign-statement-credit`), тоже через `confirmPayment`.
  Кнопка «Пропустить» — для платежей, вообще не относящихся к балансу.
- **Дедупликация повторной/пересекающейся выписки** — новая таблица
  `bank_statement_credits` хранит номер документа банка (`Doc N`) для каждой обработанной
  строки (автосовпадение, ручная привязка или явный пропуск). При повторной загрузке
  такие строки не предлагаются снова — без этого один и тот же перевод можно было бы по
  ошибке вручную привязать к двум разным счетам при пересекающихся по датам выписках
  (двойное зачисление).
- Заодно исправлено: дата в ячейке Excel иногда приходит как объект `Date`, а не текст —
  раньше рендерилась как `Fri Sep 11 2026 00:00:00 GMT+0000 (...)`, теперь как `DD.MM.YYYY`.

**Не сделано / открытый риск**: Telegram-бот `/topup <телефон> <сумма>`
(`order.controller.js` → `applyTopUp`) остаётся отдельным путём зачисления баланса и не
знает ни про `topup_requests`, ни про `bank_statement_credits` — см. задачу про риск
двойного зачисления в `tasks.md`.

Тесты: `tests/topups.test.js` (расширен — `confirmPayment`, `cancel`, `list()` с чеком и
без) и новый `tests/bank-statement.test.js` (парсинг, сопоставление, дедуп, форматирование
дат, реальный сценарий с разными номерами в двух колонках одной строки). Прогнано на
pg-mem и живым сценарием через `dev-server.js` (счёт без чека подтверждён, баланс реально
вырос; повторное подтверждение и отмена уже зачисленного отклоняются; повторная загрузка
той же выписки корректно помечает платежи как уже обработанные).

## Категории услуг и размер van — управление из админки, 16.09.2026

12 коммитов (`0f3eb62`…`aed6937`) одной продолжительной сессии по итогам живого
разбора с владельцем регистрации исполнителя, одобрения модератором и категорий
услуг на реальных скриншотах продакшена. Полные описания каждого шага —
[tasks.md](tasks.md) («Выполнено недавно», записи 16.09.2026); архитектурные
детали конфига категорий и размера `van` — [service-types.md](service-types.md);
личная ссылка модератора из Telegram — [manager-portal.md](manager-portal.md).
Здесь — только сводка, что изменилось по существу:

- **Регистрация и кабинет исполнителя**: убрана неверная подсказка про
  пополнение баланса через бота; СМС «заявка принята» теперь на языке
  регистрации, а не всегда на русском; убран лишний экран между кодом из SMS
  и кабинетом; язык самого кабинета теперь привязан к аккаунту
  (`masters.language`), а не к куке браузера — ссылка приходит по SMS и часто
  открывается в другом браузере.
- **Одобрение модератором** переехало из общего входа в `/admin` (пароль +
  SMS-2FA, не различает, кто именно одобрил) на личные одноразовые ссылки в
  Telegram-уведомлении, ведущие в двухшаговую карточку в `/manager` —
  подробности выше и в `manager-portal.md`. Менеджер, одобривший органическую
  (не реферальную) регистрацию, теперь автоматически становится за неё
  ответственным.
- **Категории услуг** (`/admin/categories`) получили удаление (создание уже
  было) и полноценное редактирование характеристик `van` (раньше полностью
  блокировалось из-за спец-поля «Размер»).
- **Размер кузова `van`** переработан целиком: добавлен тир M (был только
  S/L/XL/XXL), пороги в сантиметрах теперь настраиваются из админки без
  деплоя, а в карточке одобрения модератор вводит реальные см кузова —
  буква определяется автоматически, не выбирается вручную.
- **«Вывоз мусора»** снова отдельная категория (не `body:flatbed` внутри
  `van`) с обязательным объёмом кузова.

**Не сделано / открытый риск**: ни один из 12 коммитов не покрыт тестами в
постоянном `tests/` — проверка шла одноразовыми pg-mem-скриптами по ходу
сессии (создавались и удалялись). Перед следующей содержательной правкой
этой части — регистрация, категории, размер van, карточка модератора —
стоит прогнать полный `tests/*.test.js` и рассмотреть добавление постоянных
тестов на новые пути (`tests/manager-portal.test.js` — очевидный кандидат
для карточки одобрения и magic-link).

## Модерация и реферальные ссылки — 17.09.2026

Продолжение предыдущего дня, по итогам живой проверки владельцем на проде
(включая один реальный инцидент). Полные описания — [tasks.md](tasks.md)
(«Выполнено недавно», записи 17.09.2026); детали маршрутизации уведомлений и
личной ссылки одобрения — [manager-portal.md](manager-portal.md); детали
реферальных ссылок и партнёрской программы — [manager-partners.md](manager-partners.md).
Сводка:

- **Инцидент**: `/join` отдавал 500 на всех трёх локалях (замечено владельцем
  на проде через несколько минут после деплоя предыдущей сессии). Причина —
  общий партиал гайда для кабинета и формы регистрации стал безусловно читать
  `master.is_active`, а на `/join` мастера ещё не существует. Воспроизведено
  через `dev-server.js`, исправлено и перепроверено в течение того же захода.
- **Уведомление модератору о новой регистрации** теперь показывает, по чьей
  реферальной ссылке она пришла (или что это органика), и умеет **два**, а не
  одно действие — «только категория» / «категория и одобрить» — и в
  `/manager/review/:id`, и в `/admin/masters/:id` (там раньше одобрение вообще
  было отдельной страницей).
- **Маршрутизация уведомлений** больше не рассылает каждую регистрацию всем
  модераторам без разбора: реферальная — только владельцу ссылки; органическая
  — всем, как раньше; новый флаг «главный модератор» получает всё независимо
  от этого правила (для владельца/старшего, кому нужна полная видимость).
- **Реферальные ссылки исполнителя** (`/p/<код>`) научились нести язык
  явно (`?lang=ru|ka|en`) — раньше вели на голый `/join`, язык которого решала
  кука БРАУЗЕРА посетителя (а не язык реального получателя, которого сайт видит
  впервые). Менеджер теперь видит и копирует три готовые ссылки — в
  `/admin/managers/:id`, в `/manager` и командой `/link` (без номера) прямо в
  Telegram-боте.

**Не сделано / открытый риск**: как и в предыдущий день, ни один из этих
коммитов не покрыт постоянными тестами в `tests/` — только сессионными
pg-mem-скриптами (удалены после прохождения). Разница с предыдущим днём: на
этот раз после каждой правки прогонялся ПОЛНЫЙ существующий набор
`tests/*.test.js` (19 файлов), а не только точечная проверка новой правки —
и по ходу дела найден и исправлен один реально сломанный тест
(`tests/moderated-signup.test.js`, падал независимо от этой сессии с 15.09).

## Заявка из нескольких категорий — 18.09.2026

Реальный кейс владельца: клиенту нужны грузчики **и** машина, заявка ушла двум
группам, но закрыть можно было только всю заявку сразу. Полное описание,
правила, ограничения и грабли — [multi-category-orders.md](multi-category-orders.md);
записи по коммитам — [tasks.md](tasks.md) («Выполнено недавно»). Сводка:

- **Закрытие по категориям.** Таблица `order_category_closures`,
  `order.service.closeOrderCategory`, маршруты для клиента и модератора.
  Закрытие последней открытой категории закрывает заявку целиком прежним кодом
  (`finalizeClose`), поэтому весь код, читающий `orders.status`, не менялся.
  Журнал согласий: `ORDER_CATEGORY_CLOSED` и `CONTACT_SHARING_WITHDRAWN` со
  `scope='category:<ключ>'` вместо `'this_order'`.
- **Рассылка в закрытую категорию запрещена.** Закрыта реальная дыра:
  уникальный ключ `order_dispatches` включает размер транспорта, поэтому
  `transport/XL` после `transport/L` уходил бы исполнителям уже закрытой
  категории как платный лид.
- **Воронка по группам исполнителей.** `order.service.getOrderFunnelByCategory`
  — на группу: получили, просмотры, звонки, WhatsApp и «отклик» (звонок или
  WhatsApp) с долей от получивших. Показывается в Telegram-сообщении
  модератору и на `/admin/orders/:token`; клиентская страница не менялась.
- **Экран закрытия у клиента.** Понятные формулировки («Закрыть только: …»).
  Исправлены два дефекта, замеченных владельцем на боевой странице: сырое
  `transport` (ключ заявки ≠ slug справочника `van`, теперь
  `category.service.labelMap`) и отсутствующая иконка ✕ (мини-набор Font
  Awesome `public/fa/` не знал `xmark`; заодно `arrow-down` на `/join`).

**Тесты постоянные**, в отличие от 16–17.09: `order-category-close`,
`funnel-by-category`, `fa-subset` (плюс обновлён `consent`).

**Открытые риски**: боевая версия не проверена; грузинские строки экрана
закрытия не вычитаны носителем; `SELECT … FOR UPDATE` в `closeOrderCategory`
проверен только на pg-mem, не на реальном PostgreSQL; кабинет заказчика
(правка/дополнение/повтор заявки) — только предложение, ждёт решений
владельца (`[!]` в [tasks.md](tasks.md)).

## Управленческая аналитика

Все вкладки находятся на `/admin`. Подробные определения:
[admin-analytics.md](admin-analytics.md).

| Вкладка / URL | Содержание |
|---|---|
| Операции и спрос — `/admin` | Подтверждённые заявки, заявки с контактом, конверсия после рассылки, среднее/медиана/P90 ожидания, очередь вмешательства, воронка и баланс категорий |
| Исполнители — `/admin?tab=providers` | Назначенный менеджер, доступный источник по промокоду, баланс, пополнения/списания, уникальные лиды, открытия, контакты и среднее время до контакта; фильтр по менеджеру |
| Менеджеры — `/admin?tab=managers` | Состав команды, новые регистрации, допуск к рассылке, суммарные балансы, пополнения/списания, контакты, среднее время, повторные пополнения; ссылки на карточки и команду |
| Финансы — `/admin?tab=finance` | Пополнения, бонусы, коррекции, списания за рассылки и каталог раздельно; текущий остаток |

Периоды: сегодня, 7/30/90 дней, произвольные даты до 366 дней. Дни считаются
по Тбилиси. Автообновление раз в 45 секунд можно отключить; в скрытой вкладке
запросы приостанавливаются. Ошибка обновления сохраняет предыдущие данные
и показывает их возраст. JSON-фрагмент требует администраторскую сессию.

Ключевые границы интерпретации:

- Контакт означает нажатие звонка или WhatsApp. Это не подтверждение разговора
  или выполненной работы. Открытия показаны отдельно.
- Воронка заявок — когорта по дате создания; события наблюдаются до момента
  отчёта. Старые когорты имели больше времени для отклика, чем новые.
- Подтверждение в текущем агрегаторе определяется по статусу, отличному от
  `unverified`, а не отдельной выборкой журнала OTP.
- Воронка исполнителя — пары исполнитель/заявка по списаниям за лиды периода.
  Один звонок и WhatsApp по одной паре не удваивают контакт.
- Время исполнителя отсчитывается от списания; время заявки — от первой рассылки.
  Средние и перцентили исключают неотреагировавших; они видны в очереди проблем.
- Финансы отбираются по дате операции. Пополнения и списания — разные потоки;
  списания могут расходовать бонусы.
- Назначенный менеджер — текущая ответственность. Для комиссий используется
  отдельно закреплённый referral_manager_id; старый источник по промокоду
  остаётся подсказкой и сам по себе не создаёт комиссий для прежней базы.
- Допуск к рассылке учитывает статус, подписку, срок и баланс; он не подтверждает
  свободное время исполнителя. Срез категорий не является картой районов или размеров.
- Категории пересекаются; контакт приписан заявке целиком, а не отдельной рассылке.

## Заявка: возврат на исправление

Основная последовательность:

`unverified → pending_review → new → closed`

Доработка до первой рассылки:

`pending_review → needs_revision → pending_review`

1. Модератор открывает карточку в админке, в том числе по кнопке из Telegram.
2. Указывает причину (5–1000 символов). Заявка получает `needs_revision`;
   клиенту отправляется SMS со своей ссылкой. Ошибка SMS показывается администратору.
3. Владелец исправляет описание (10–5000 символов) и адрес (до 200).
   Уже прикреплённые файлы сохраняются; новых загрузок в этой форме нет.
4. Отправка требует owner-cookie, CSRF и текущую версию заявки. Статус возвращается
   в `pending_review`, старые переводы очищаются. Модераторы получают исправленный
   оригинал; повторный автоматический перевод в этом обработчике не выполняется.
5. Рассылка запрещена до повторной проверки. Закрытые и уже распределённые
   заявки через этот сценарий редактировать нельзя.

Постоянный отказ с отдельным статусом `rejected` не вводился: реализован именно
возврат на доработку. Закрытие остаётся отдельным действием.

Новые POST-маршруты:

- `/admin/orders/:token/request-revision` — администраторская сессия + CSRF.
- `/api/orders/:token/resubmit` — владелец + CSRF + версия.

Транзакция рассылки и возврат изменяют одну строку orders; версия защищает от
старых форм и Telegram-кнопок. События `ORDER_REVISION_REQUESTED` и
`ORDER_RESUBMITTED` пишутся в журнал вместе с изменением состояния.

## Код и миграция релиза аналитики e0839c1

- `src/services/dashboard.service.js` — выборки и расчёты аналитики.
- `src/views/admin/overview.ejs`, `_dashboard.ejs`, `_people-analytics.ejs` — экран,
  вкладки и автообновление.
- `src/services/orderRevision.service.js` — доработка и проверка версии.
- `src/config/order-revision-copy.js`, `src/views/partials/order-revision.ejs` —
  клиентская форма и тексты трёх языков.
- Контроллеры, маршруты, рассылка и Telegram обновлены для нового статуса.
- В `schema.sql` добавлены `revision_reason`, `revision_requested_at`,
  `revision_version` и индексы по созданию/статусу заявок и времени транзакций.

Стандартный старт применяет schema.sql и schema.postgres.sql. Изменения
добавочные и идемпотентные. Релиз не пересчитывает тарифы, остатки или старые списания.

## Воспроизведение проверок

Из корня проекта:

```powershell
node tests/partners.test.js
node tests/dashboard.test.js
node tests/consent.test.js
node tests/topups.test.js
node tests/bank-statement.test.js
node tests/welcome-bonus.test.js
node tests/language-switch.test.js
node tests/master-session.test.js
npm run build
```

Тесты аналитики используют pg-mem и локальный Express, без отправки реальных SMS.
Проверены SQL, даты, пустые данные, дедупликация, четыре вкладки, HTTP, CSRF,
владение, версии, доработка, запрет рассылки, откат при ошибке журнала и
экранирование клиентского текста. Проверки pg-mem не заменяют нагрузочные тесты
и проверку конкурирующих транзакций на реальном PostgreSQL.

## Следующие потребности, ещё не реализованные

Конкретные задачи с критериями готовности — в [tasks.md](tasks.md) (вход
мастера, IBAN, карточная оплата BOG). Ниже — более общий бэклог идей без
привязанного статуса выполнения:

1. Подтверждение выбора исполнителя через платформу, факта выполнения и суммы сделки.
2. Полная воронка посетитель → каталог → OTP → раскрытие номера и рекламная атрибуция/CAC.
3. История переназначений ответственного менеджера; источник для комиссии уже
   закрепляется отдельно, изменения партнёрской программы пишутся в partner_audit.
4. Журнал попыток/ошибок доставки по каждому получателю; подтверждение доставки,
   где его предоставляет канал. Списания не доказывают доставку на устройство.
5. Заявленная доступность исполнителей, качественная география, срезы по размерам.
6. Настраиваемые пороги ожидания по категориям и отдельные автоматические алерты.
7. Нагрузочная проверка аналитики; сейчас ряд отчётов загружает базу исполнителей
   и агрегаты истории целиком, отдельной пагинации таблицы аналитики нет.
8. Кабинет заказчика: правка до рассылки, дополнение и «добавить услугу» после
   неё, «повторить заявку», вход по номеру с любого устройства. Предложение и
   вопросы владельцу — [multi-category-orders.md](multi-category-orders.md) §9 и
   `[!]` в [tasks.md](tasks.md); не реализовано.
9. Экран «Пульс» в админке: пульс заявок и исполнителей, время до открытия
   ссылки и отклика по каждому исполнителю, доля попаданий на закрытую заявку,
   графики. Почти все данные уже записываются; предложение и вопросы владельцу —
   `[!]` в [tasks.md](tasks.md); не реализовано.

Общие реестры и карточки остаются доступны. Локальный HANDOFF.md хранит историю
сессий и исключён из Git; актуальная переносимая точка входа — этот документ.
