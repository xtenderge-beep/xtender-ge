# Задачи Xtender

Единственный актуальный список «что делать дальше». Не журнал истории —
для истории решений и деталей уже сделанного см.
[current-state.md](current-state.md) (в git) и локальный `HANDOFF.md`
(если он есть в этой среде — он исключён из git и может отсутствовать).

## Протокол — обязательно для каждой новой сессии

Не важно, какой инструмент/модель работает с этим репозиторием (Claude Code,
другая среда, другой человек) — правила одни:

1. **Перед началом любой работы** прочитайте этот файл целиком, не только
   заголовки. Задача может быть заблокирована, частично сделана или отменена —
   это видно только из заметки внутри неё.
2. Если пользователь не указал конкретную задачу — берите первую невыполненную
   не в блоке «Выполнено» сверху вниз.
3. **Сразу после того как задача сделана** — отметьте её здесь же (`[x]`,
   дата, коммит, короткий итог) и переместите в раздел «Выполнено». Не
   откладывайте это на «потом» и не полагайтесь на память модели — если
   сессия прервётся до этого шага, следующая сессия увидит задачу как
   невыполненную и может повторить работу или потерять контекст.
4. Если задача оказалась не такой, как описана (сделали иначе / решили не
   делать / выяснилось, что не нужна) — не удаляйте запись, смените статус
   и опишите, что изменилось и почему. Пустая история хуже неидеальной.
5. Если для продолжения нужен ответ или решение пользователя (не техническое,
   а продуктовое/организационное) — статус `[!]`, чётко сформулируйте вопрос
   внутри задачи. Не гадать и не выполнять вместо пользователя.
6. Новую задачу — дописывайте в конец активного списка с той же структурой:
   заголовок, статус, контекст (что и почему), критерий готовности.
7. **Локальная рабочая копия может редактироваться параллельно другой
   сессией прямо сейчас** (не только через git — файлы на диске). Перед
   `git add`/коммитом смотрите `git status` и стейджите только файлы,
   которые реально относитесь к вашей задаче — не `git add -A`/`.`.
   Прецедент 2026-09-15: во время работы над задачей ниже про телефон в
   рабочей копии обнаружились чужие незакоммиченные правки (`i18n.js`,
   `index.ejs`, `input.css` — карусель примеров на главной) и отдельный уже
   запушенный коммит `ecdc6c1` от другой сессии — оба не имели отношения к
   текущей задаче, тронуты не были.

**Легенда статусов**: `[ ]` не начато · `[~]` в процессе (см. заметку, где
остановились) · `[!]` заблокировано (см. заметку, чего ждём) · `[x]` сделано
· `[-]` отменено (см. заметку, почему)

---

## Активные задачи

### [!] Карточная оплата через BOG — интеграция

Заблокировано: заявка на подключение отправлена в банк, ответа/доступа к
документации или песочнице пока нет (на 15.09.2026).

Заранее подготовлено (не зависит от доступа к BOG, 2026-09-15, commit
`b104bfa`): таблица `card_payments` в `schema.sql` — `status`
(`pending/paid/failed/cancelled`), `external_id` (id платежа у шлюза),
`balance_transaction_id` — та же цепочка зачисления баланса, что и у
ручных банковских пополнений (`masterService.adjustBalance` →
`balance_transactions`, см. `topup_requests` как образец). Проверено на
pg-mem: `paid` без привязанной транзакции и повтор `external_id`
корректно отклоняются констрейнтами.

Намеренно НЕ написан HTTP-клиент к BOG, вебхук или проверка подписи —
без реальной документации это будет угадывание формата запросов/ответов,
почти наверняка придётся переписывать.

**Когда появится доступ к документации BOG**:
1. Прочитать их API-доки: создание платежа, callback/webhook, проверка
   статуса, формат подписи/авторизации вебхука.
2. `src/services/bogPayment.service.js` — клиент + обработка callback,
   зачисление через существующий `masterService.adjustBalance`.
3. Роут вебхука (вне CSRF-защищённой зоны, как и Telegram-вебхук).
4. Кнопка «Оплатить картой» в кабинете исполнителя рядом с текущим `/topup`.
5. Тест на pg-mem по образцу `tests/topups.test.js`.

**Критерий готовности**: исполнитель может пополнить баланс картой через BOG,
статус платежа виден в админке, баланс зачисляется автоматически без участия
модератора, есть тест.

### [ ] IBAN в кабинете исполнителя — сверить с офертой

`src/config/payment.js` и `src/config/legal.js` (статические дефолты в коде)
уже совпадают друг с другом: `GE95BG0000000613339218`. Но реальное значение,
которое видит исполнитель на странице пополнения, берётся из БД
(`app_settings.payment_details`, редактируется на `/admin/settings` →
реквизиты) и может быть переопределено более старым значением
(`GE22BG0000000612888318` фигурировало в предыдущей версии документации).
У этой сессии нет доступа к продакшен-БД, чтобы проверить текущее значение.

**Критерий готовности**: зайти на `/admin/settings` на проде, посмотреть
поле IBAN в блоке реквизитов. Если там `GE22BG...318` — заменить на
`GE95BG0000000613339218` прямо в форме (без деплоя кода). Если уже верное —
просто отметить задачу выполненной с пометкой «уже было верно».

### [ ] Вход мастера — восстановить нормализацию телефона

Регрессия из коммита `ae9a1c3` (2026-09-14, изменения через другую модель).
`master.controller.js` → `loginRequestCode`/`loginVerify`: раньше номер
приводился к канонической форме через `toE164()` перед поиском мастера в БД
(`masterService.getMasterByPhone`, точное сравнение `WHERE phone = $1`).
Теперь используется сырой ввод, провалидированный строгим регексом
`+995XXXXXXXXX` (`isGeorgianPhone`) — без нормализации. Раньше номер без `+`
или без кода страны (`995...`, просто 9 цифр) успешно находил мастера; теперь
такой ввод отклоняется как «не грузинский номер», даже если после
нормализации он оказался бы тем же самым зарегистрированным номером.

Живой тест на dev-server (2026-09-15) подтвердил: `+995510000000` входит,
`995510000000` и `510000000` (тот же номер без форматирования) — нет.

Поля логина на сайте подставляют `+995` заранее и валидируют формат на
клиенте, поэтому обычный вход через сайт не задет. Риск — автозаполнение
браузером/менеджером паролей старым сохранённым форматом номера, либо
сторонние клиенты API.

Не чинили: не совпадало с поломкой, которую в той сессии искал пользователь
(это был краш-луп миграции, см. «Выполнено» ниже).

**Критерий готовности**: `loginRequestCode`/`loginVerify` снова нормализуют
номер через `toE164()` перед `getMasterByPhone`, сохраняя при этом
Georgian-only проверку формата ПОСЛЕ нормализации (не до) — так, чтобы
`995...`/`510000000`/`+995 510 000 000` находили того же мастера, что и
`+995510000000`. Добавить кейс в `tests/georgian-phone.test.js` или новый
тест, воспроизводящий сценарий из живой проверки выше.

### [!] Риск двойного зачисления: Telegram-бот `/topup` не знает о счетах

Обнаружено 2026-09-16 при обсуждении с владельцем сверки выписки на `/admin/receipts`
(см. `current-state.md`, раздел «Сверка пополнений...»). `order.controller.js` →
`applyTopUp` (команда `/topup <телефон> <сумма>` в Telegram) зачисляет баланс напрямую
через `masterService.topUpBalance()`, минуя `topup_requests` и `bank_statement_credits`.

Если модератор зачислит реальный перевод этой командой, а тот же перевод затем попадёт
в загруженную банковскую выписку и будет вручную привязан к какому-то счёту на
`/admin/receipts` (или наоборот — сначала привязка, потом бот) — один и тот же реальный
перевод зачислится дважды. Раньше это было маловероятно (старая `/admin/receipts` только
помечала статус чека и не двигала баланс сама); после сегодняшних изменений кнопки на
этой странице стали полноценным альтернативным путём зачисления, так что риск стал
реальным. Дедупликация по номеру документа банка (`bank_statement_credits`) защищает
только от повторной обработки ОДНОЙ И ТОЙ ЖЕ выписки — она не знает о зачислениях,
сделанных через Telegram-бота.

Нужно решение владельца, не техническое: (а) перестать использовать Telegram-`/topup`
для платежей, у которых уже есть созданный счёт с номером в назначении, оставив бота
только для случаев без счёта, или (б) при зачислении через бота проверять, не похож ли
телефон+сумма на уже существующий неоплаченный счёт, и предупреждать модератора.

**Критерий готовности**: решение зафиксировано (выбран вариант, либо риск сознательно
принят с описанием почему) — при необходимости реализовано и покрыто тестом.

### [!] Внести исправленный юристом/моделью текст Оферты

Заблокировано: владелец 2026-09-17 отправил грузинский текст Оферты (плюс сверку
ru/en) на вычитку — отдельно юристу и отдельно другой AI-модели, задания
подготовлены в чате той сессии (не в репозитории, восстановить нечем, кроме
памяти владельца/переписки). Ответа пока нет.

Механизм для внесения правок уже готов (см. «Выполнено» — `/admin/legal`,
commit `4aaddbe`): вставить исправленный текст в textarea по языкам и
сохранить — версия документа поднимется автоматически, деплой не нужен.

**Критерий готовности**: правки от юриста/модели получены, расхождения между
их рекомендациями (если есть) разрешены владельцем, текст внесён через
`/admin/legal`, версия документа выросла. Главный риск, ради которого всё
затевалось — грузинский текст никогда не проверял грузинский юрист/носитель
(п. 1.6 оферты делает его юридически приоритетным) — закрыт.

---

## Выполнено недавно

- **[x] 2026-09-18** — закрытие заявки по отдельным категориям. Заявка может
  уйти сразу нескольким категориям (`orders.target_categories`, растёт по мере
  рассылки — `dispatch.service.dispatch`), но закрывалась раньше только целиком
  (`order.service.closeOrder`, дёргают и клиент с `/order/:token`, и модератор
  из `/admin/orders/:token`) — владелец описал реальный кейс: клиент нашёл
  грузчиков, но машина ему ещё нужна, а закрыть можно было только всё сразу.
  Новая таблица `order_category_closures` (order_id+category, `schema.sql`) —
  один ряд на закрытую категорию. `order.service.closeOrder` разбит на
  `finalizeClose` (общее ядро) + новую `closeOrderCategory(token, category,
  opts)`: закрывает одну категорию, не трогая остальные; когда закрываемая
  оказалась последней открытой — закрывает заявку целиком тем же кодом, что и
  раньше (весь код, читающий `orders.status`, не изменился). Юридический
  журнал (`sms_consent_logs`) при частичном закрытии пишет
  `CONTACT_SHARING_WITHDRAWN` с `metadata.scope='category:'+category` вместо
  `'this_order'` — согласие отзывается только на эту категорию. Новые роуты
  `POST /api/orders/:token/close-category` (клиент, `order.controller.js`) и
  `POST /admin/orders/:token/close-category` (модератор,
  `admin.controller.js`) — копии существующих `/close`, с проверкой, что
  категория реально входит в `target_categories`. У специалиста, открывшего
  заявку по своей ссылке (`?master=<id>`), теперь тоже учитывается его
  категория: `order.ejs` показывает «эта часть заявки уже закрыта», если
  закрыта именно его категория, даже когда заявка в целом ещё активна для
  другой; то же для `master-status.ejs` («мои заявки» в кабинете исполнителя)
  — `master.service.getMasterLeads` получил новый параметр `masterCategory` и
  джойн на `order_category_closures`. UI: `order.ejs` (клиент) — при больше чем
  одной ещё открытой категории список кнопок «закрыть <категория>» + отдельная
  «закрыть всю заявку»; при одной оставшейся — как раньше, один общий button
  (закрытие последней и так закрывает всё). То же в `admin/order-detail.ejs`.
  Однокатегорийные и ещё не распределённые заявки — поведение не менялось
  вообще (та же ветка кода, что и до правки). Не тронуто сознательно: список
  `/my-orders` и текст модераторского сообщения в Telegram по-прежнему
  показывают только общий статус — точная картина по категориям видна на
  `order.ejs`/`/admin/orders/:token`; кнопки закрытия в самом Telegram-боте не
  было и раньше. Проверено: весь `tests/*.test.js` (18 файлов, включая
  подправленный `consent.test.js`, который рендерит `order.ejs` напрямую и не
  знал о новых locals); отдельный pg-mem-скрипт (не сохранён в `tests/`) —
  частичное закрытие не трогает статус заявки, лог согласия скоупится по
  категории, повторное закрытие той же категории — молчаливый no-op, закрытие
  последней категории закрывает всё ровно как старый `closeOrder`
  (`ORDER_CLOSED` пишется один раз), `getMasterLeads` по-разному отвечает для
  мастера закрытой и открытой категории той же заявки. Формальный автотест
  оформлен позже в тот же день — `tests/order-category-close.test.js`
  (частичное/полное/повторное закрытие, скоуп журнала согласия, закрытие
  модератором, однокатегорийная и нераспределённая заявки).
  **Дыра, найденная при этом:** `dispatch.service.preview` смотрел только
  `orders.status`, а уникальный ключ `order_dispatches` включает размер
  транспорта — поэтому после закрытия категории `transport` модератор мог
  запустить `transport/XL` после `transport/L`, и исполнители уже закрытой
  категории получили бы платный лид. Теперь `preview` (а значит и `dispatch`)
  отказывает для закрытой категории («Эта категория заявки уже закрыта»), а в
  форме рассылки `admin/order-detail.ejs` закрытые категории не
  предлагаются; тест на это падает без правки.

- **[x] 2026-09-18**, commit `19f6f8a` — карточка быстрого одобрения
  `/manager/review/:id` (см. «Личная ссылка одобрения из Telegram» в
  `docs/manager-portal.md`) была реализована исключительно как получатель
  личной одноразовой Telegram-ссылки (`issueMagicLink`) — сама страница
  специалиста в веб-кабинете (`/manager/masters/:id`, `detail.ejs`) на неё
  не ссылалась вообще. Менеджер без Telegram-уведомлений (не привязан бот,
  выключен, или просто пропустил сообщение) не имел в кабинете ни одного
  способа назначить категорию и одобрить уже закреплённого за ним
  специалиста — только «Заметка» и «Заблокировать» (замечено владельцем на
  реальном специалисте «На модерации», скриншоты кабинета). Теперь
  `detail.ejs`, пока специалист `!is_active && !is_banned`, показывает
  карточку «Заявка на модерации» со ссылкой на `/manager/review/:id`. Прав
  не расширяет — `managerPortal.service.reviewGet`/`assignCategory`/
  `approvePending` и так уже проверяют, что специалист закреплён именно за
  этим менеджером. Проверено `tests/manager-portal.test.js`.

- **[x] 2026-09-18**, commit `96f57bc` — защита префикса `+995` в полях
  телефона (`public/js/georgian-phone.js`, введена 2026-09-15 коммитом
  `1169720`) ловила только ручное редактирование и вставку через
  `beforeinput`/`paste`. Подсказка мобильной клавиатуры «заполнить
  сохранённым контактом» подставляет значение поля напрямую, минуя оба
  события, — префикс пропадал целиком (воспроизведено владельцем на
  реальной регистрации по ссылке, скриншот WhatsApp: поле показывало голые
  9 цифр без `+995`). Добавлена `repairPrefix()`: на каждое `input`/
  `change`/`blur` проверяет, начинается ли значение с `+995`, и если нет —
  восстанавливает префикс перед оставшимися цифрами, независимо от способа
  изменения поля. Дыры в безопасности не было — сервер (`src/config/
  phone.js`) и раньше строго отклонял номер без `+995` при выпуске OTP; баг
  был чисто в UX (форма отклонила бы номер без объяснения, откуда пропал
  префикс). Проверено `tests/georgian-phone.test.js` (полный набор) и
  вручную — логика репарации на характерных значениях автозаполнения.

- **[x] 2026-09-18**, commit `2e7339e` — реферальная ссылка менеджера не
  показывалась в веб-кабинете (`/manager`), если токен ещё не был создан ни
  одним из трёх ленивых путей (кнопка в `/admin/managers/:id`, команда
  `/link` боту — commit `459c9b9`, CRM-лид типа «провайдер»). У только что
  заведённого менеджера ни один из них ещё не случался — блок просто не
  рендерился (`dashboard.ejs` рисует его только `if(manager.referral_token)`),
  без единого пояснения на странице. `managerPortal.routes.js`: `GET
  /manager` теперь сам вызывает `partner.service.createLink()` перед
  рендером — та же ленивая функция, что и у бота (идемпотентна: у менеджера
  с уже созданным токеном просто возвращает его без изменений). Проверено
  `tests/manager-portal.test.js` и `tests/partners.test.js`.

- **[x] 2026-09-17**, commit `4aaddbe` — продолжение задачи выше («Внести
  исправленный юристом/моделью текст Оферты»): сначала
  подготовили задание на вычитку грузинского текста Оферты юристом/другой
  моделью (сам текст и задание — в чате сессии, не в репозитории), затем
  владелец захотел сам вносить правки через админку, без разработчика.
  Новая страница **`/admin/legal`** — по 3 textarea (ka/ru/en) на Оферту и
  Политику в свёрнутых аккордеонах, простой построчный формат вместо JSON
  (`## Заголовок`, `- пункт`, `[[REQUISITES]]`, обычная строка — абзац;
  `src/config/legalTextFormat.js`, парсер проверен на round-trip с реальным
  сегодняшним текстом всех 6 документов, не игрушечными примерами). Хранится
  в `app_settings` (`legal_terms`/`legal_privacy`, `legalContent.service.js`)
  — тот же паттерн, что цена лида и `payment_details`, без in-memory кэша по
  той же причине: лишний SELECT дешевле риска сломать digest-блокировку
  согласия устаревшим кэшем. **Версия документа поднимается автоматически**
  на каждое сохранение (MINOR+1, дата — сегодня) — раньше это была ручная
  дисциплина разработчика в `legal.js`, теперь поднять/забыть поднять
  нельзя. `legal-content.js`/`legal.js` не удалены — фолбэк/сид на случай
  пустой БД. `consent.service.js` (`bundle`/`acceptedRequest`) стал `async`
  — 7 точек вызова в `public.routes.js`/`crmPublic.routes.js`/
  `otp.controller.js`/`master.controller.js` поправлены на `await`; ни один
  EJS-шаблон не менялся — они как получали, так и получают тот же формат
  locals, просто вычисленный на слой дальше. `SERVICE_REQUISITES` (банк/ИП)
  сознательно НЕ вынесены в этот проход — известный второй источник тех же
  банковских данных (`payment_details` в `/admin/settings`, см. задачу про
  IBAN ниже), объединение источников — отдельная задача. Проверено вживую
  через `dev-server.js`: правка текста через `/admin/legal` → версия
  бампнулась → правка сразу видна на `/terms` и на шаге «Договор» на
  `/join` → digest, снятый ДО правки, корректно отклонён 409 «условия
  обновились», снятый ПОСЛЕ — принят; регистрация после правки архивирует
  именно новый текст с новой версией в `sms_consent_logs` (проверено в
  `/admin/consent`). Попутно всплыли 3 регрессии в существующих тестах от
  перехода `bundle()` на async (`crm.test.js`, `technical-routing.test.js` —
  забытый `await`; `master-session.test.js` — тест не стабил БД, а один из
  путей теперь её трогает) — исправлены, не обойдены. Новый
  `tests/legal-editor.test.js` (round-trip парсера, фолбэк на пустую БД,
  отказ на пустом языке, авто-бамп версии, что `bundle()` подхватывает
  правку без кэша). Полный набор `tests/*.test.js` — 18 файлов — проходит.

- **[x] 2026-09-17**, commits `f03e020`, `b21a215` — владелец попросил
  юридически усилить согласие исполнителя с Офертой при регистрации на
  `/join`: раньше — обычный чекбокс рядом со ссылкой на `/terms`, как
  доказательство «был ознакомлен» слабое. `f03e020` — чекбокс Оферты больше
  нельзя поставить напрямую: открывает модальное окно с полным текстом
  документа, кнопка принятия задизейблена, пока текст не проскроллен до
  конца (мобильная подсказка «Прокрутите вниз» + градиент-затухание,
  пропадают насовсем после первого достижения конца). `b21a215` — по
  уточнению владельца модалку заменили на **отдельный пятый шаг визарда**
  (Услуги → Город → Языки → Контакт → Договор, было 3 шага с городом+языками
  и контактом+согласием на одном экране каждый) — та же прокрутка, но не
  поверх шага «Контакт», а полноценным экраном; кнопка «Получить код» и есть
  кнопка принятия, одно действие вместо двух. Сервер не доверяет клиенту
  факт скролла на слово — `consentService.acceptedRequest(req,'provider',
  {requireScroll:true})` требует `termsScrolled===true` в теле запроса,
  иначе 400 ещё до отправки SMS-кода; таймстамп подтверждения — серверный.
  Факт архивируется в `sms_consent_logs.metadata`
  (`terms_scroll_confirmed`/`_at`) и виден в `/admin/consent` отдельной
  строкой Double Opt-In карточки. Применено только к Оферте исполнителя
  (высокие ставки — платный сервис с автосписанием); Политика и клиентский
  флоу заявки остались простым чекбоксом без gate — осознанное решение:
  заказчик не платит Xtender напрямую, лишний шаг бьёт по самой
  конверсионно-чувствительной части воронки без соразмерной юридической
  выгоды. Обсуждали и решили НЕ форсировать показ грузинской версии
  не-грузиноязычным пользователям (слабее для информированного согласия) —
  грузинский текст в любом случае архивируется целиком в
  `metadata.snapshot.documents.terms.body.ka` при каждом согласии независимо
  от показанного языка. Проверено вживую через `dev-server.js` + Playwright
  на мобильной ширине (iPhone-12): кнопка заблокирована сверху и на
  середине скролла экрана «Договор», разблокируется только по факту
  достижения конца; оба серверных гейта (отсутствие `termsScrolled` и
  протухший digest) дают понятные ошибки вместо тихого пропуска. Полный
  набор `tests/*.test.js` (17 файлов на тот момент, включая почищенный
  `tests/consent.test.js` — рендер `join.ejs` требовал новых locals для
  встроенного текста Оферты) проходит.

- **[x] 2026-09-17**, commit `459c9b9` (+ доки `21f5000`) — команда `/link` без
  номера в боте (раньше работала только как `/link +995...` для заказчика, без
  номера просто показывала подсказку по формату и больше ничего не делала) теперь
  присылает менеджеру его постоянную реферальную ссылку регистрации исполнителя
  сразу на 3 языках (те же варианты `/p/<код>?lang=`, что и в предыдущей задаче) —
  можно скопировать нужный вариант прямо из Telegram, не открывая `/admin` или
  `/manager`. Токен создаётся автоматически при первом обращении, если его ещё не
  было. `/link +995...` не тронут. Проверено сессионным pg-mem-скриптом через
  реальный `telegramWebhook` (не только внутреннюю функцию): бессрочный токен
  создаётся один раз и переиспользуется при повторных `/link`; номерной вариант и
  подсказка при мусорном вводе не сломаны. Автотеста в `tests/` нет.

- **[x] 2026-09-17**, commit `22b7a8c` — реферальная ссылка исполнителя
  (`/p/<код>`) раньше всегда вела на голый `/join` без указания языка; если у
  зашедшего уже была кука языка с прошлого визита БРАУЗЕРА — сайт её уважал,
  поэтому у владельца (тестировал из своего же браузера с накопленной
  `lang=ru`) ссылка стабильно открывалась по-русски. Для реального нового
  контакта куки обычно нет вообще — угадать его язык нечем. Раз угадать
  нельзя, а менеджер, отправляющий ссылку, обычно знает язык получателя —
  `/p/<код>` теперь принимает `?lang=ru|ka|en` и ведёт сразу на нужный вариант
  `/join` (ru/en — префиксом URL, ka — явной меткой `?lang=ka`, чтобы старая
  кука не перебила). И в `/admin/managers/:id`, и в `/manager` менеджеру теперь
  показываются три готовые ссылки (по одной на язык) вместо одной. Без явного
  `?lang=` поведение прежнее (кука/дефолт ka). Проверено, подняв реальный `app.js`
  на pg-mem и пройдя цепочку по HTTP: каждый `?lang=` ведёт на верный таргет,
  который реально рендерится 200; без подсказки старое поведение (в т.ч. случай
  владельца — ссылка без `?lang=` + кука `ru` в браузере) не изменилось. Полный
  набор `tests/*.test.js` (19 файлов) проходит.

- **[x] 2026-09-17**, commit `75f6fc9` (+ доки `edc5fde`) — с тремя менеджерами
  (Артур, Виктория, Арминe) рассылка уведомления о новой регистрации всем подряд
  стала шумом: если регистрация пришла по реферальной ссылке конкретного
  менеджера, только он и может её одобрить (`referral_manager_id` уже блокирует
  чужую заявку на одобрение), значит остальным двум уведомлять о ней незачем.
  Теперь `telegram.service.notifyModeratorNewMaster`: реферальная регистрация →
  только владельцу ссылки (если он реально может получить сообщение — активный
  модератор с привязанным Telegram; иначе, чтобы не потерять заявку молча, откат
  на рассылку всем). Органическая (без ссылки) — всем активным модераторам, как
  раньше. Новый флаг `managers.is_head_moderator` («+ главный» в `/admin/managers`,
  бейдж 🎖) — получает КАЖДУЮ регистрацию дополнительно к правилам выше (для
  Артура, владельцу нужна видимость всего потока). Проверено pg-mem-скриптом с
  подменённым `axios` (без реальных обращений к Telegram): реферальная — только
  владельцу + главному; органическая — всем троим без дублирования главному;
  недоступный по Telegram владелец ссылки — откат на рассылку всем;
  `is_head_moderator` переживает существующие переключатели «модератор»/
  «вкл-выкл» (у них общая форма со скрытыми полями текущего состояния — при
  добавлении флага все три формы пришлось обновить, иначе один тумблер тихо
  сбрасывал другой). Полный набор тестов проходит.

- **[x] 2026-09-17**, commit `f1f35ba` — три находки по итогам разбора модераторского
  флоу с владельцем:
  1. Telegram-уведомление о новой регистрации не показывало, по чьей реферальной
     ссылке она пришла — менеджер, привязывающий заявку к себе в БД
     (`partner.service.bindNew`), не попадал в JS-объект `master`, который уже
     ушёл в уведомление. Теперь `master.service.registerMaster` дозапрашивает
     `referral_manager_id`/имя после `bindNew` и уведомление показывает
     `🔗 По ссылке: <имя>` или «Без реферальной ссылки (органика)».
  2. И `/admin/masters/:id`, и `/manager/review/:id` раньше давали только
     «назначить категорию» ИЛИ «назначить и одобрить одним действием» — не оба
     варианта; на `/admin` одобрение вообще было отдельным действием на другой
     странице (общий список `/admin/masters`). Теперь везде по две явные кнопки:
     «Сохранить»/«Только категория» (остаётся на модерации) и «Сохранить и
     одобрить»/«Категория и одобрить» (одним кликом). `managerPortal.service`:
     общее ядро `assignCategoryCore`, `assignCategory`/`approvePending` каждый
     пишет свой тип события (`assign_category`/`approve`), не задваивая лог.
  3. Попутно найден и исправлен реальный баг: `tests/moderated-signup.test.js`
     (открытая задача с 2026-09-15) падал с `ReferenceError: t is not defined` —
     тест рендерил script-блок `join.ejs` без локали `t`, которую использует
     `provider_notifications_data`. Добавлен `t:i18n.translate(lang)` в вызов.
  Проверено pg-mem-скриптом (привязка имени с рефералом/без; assign-only не
  одобряет и захватывает `manager_id`; второй менеджер не может перехватить уже
  закреплённую заявку; оба события пишутся раздельно; все затронутые EJS
  рендерятся в обоих состояниях мастера; admin-контроллер с `thenApprove=1`
  назначает и одобряет одним запросом) и впервые в этой сессии — полным набором
  `tests/*.test.js` (все 19 файлов, включая только что починенный).

- **[x] 2026-09-17**, commit `289470c` — `/join` отдавал 500 на всех локалях
  (`Internal server error`, замечено владельцем на проде). Причина: партиал
  `partials/provider-guide.ejs`, общий для кабинета (`master-status.ejs`, где есть
  `master`) и формы `/join` (где `master` ещё не существует), после вчерашней
  правки «сворачивать гайд, пока профиль на модерации» стал безусловно читать
  `master.is_active` — на `/join` это `ReferenceError`. Воспроизведено локально
  через `dev-server.js` (in-memory Postgres) — увидел точный стек, поправил на
  `typeof master === 'undefined' || !master || !master.is_active`. Проверено:
  `/join` на всех трёх локалях снова 200; поведение кабинета (свёрнут/раскрыт для
  активного/ожидающего мастера) не изменилось.

- **[x] 2026-09-17**, commits `cd03f93`, `ee3d231` — два мелких UX-замечания по
  карточке специалиста в `/admin/masters/:id`: (1) поле «Размер кузова» раньше
  показывалось и было доступно для любой категории, хотя относится только к
  «Перевозки» — теперь скрыто/disabled, если выбрана не `transport` (переиспользован
  уже существующий JS-переключатель полей по категории). (2) Размеры кузова
  отображались голыми числами `400×190×200 см` без подписи, что есть что —
  `vanSizeSpec()` (единственный источник этой строки, используется и в admin, и в
  карточке одобрения менеджера, и в кнопках Telegram) теперь отдаёт
  `Д400×Ш190×В200 см`.

- **[x] 2026-09-16**, commits `aed6937`, `67844cd`, `082eaa4` — размер кузова `van`
  доведён от «модератор угадывает букву» до «вводит см, буква сама определяется, а
  пороги правятся из админки». Три связанных шага одной сессии:
  1. `082eaa4` — `/admin/categories/van` раньше прятал ВСЕ характеристики категории
     (не только `size`), если у неё было спец-поле `input:'size'`. Разделил: `size`
     по-прежнему не редактируется тут (ставится legacy-колонкой), но «Тип кузова»/
     «Гидроборт»/«С грузчиками» теперь редактируются как у любой другой категории —
     `category.service.save()` разделяет `previous.fields` на заблокированные и
     обычные, парсит только вторые.
  2. `67844cd` — см-пороги S/M/L/XL/XXL (были захардкожены в `serviceTypes.js`, правка
     требовала деплоя) переехали в `app_settings` (ключ `van_size_thresholds`, тот же
     паттерн, что цена лида) с фолбэком на дефолты. Форма на той же странице категории.
     Буквы фиксированы кодом (CHECK-констрейнты в schema.sql), настраиваются только
     сантиметры за ними.
  3. `aed6937` — в карточке быстрого одобрения модератора (`/manager/review/:id`,
     см. `docs/manager-portal.md`) буква S/M/L/XL/XXL больше не выбирается вручную:
     менеджер вводит длину/ширину/высоту кузова (как написано у исполнителя в
     описании), сервер определяет букву через `deriveVanSize` по актуальным (из
     настроек) порогам. Клиентский JS считает то же самое только для превью до
     отправки — на запись не влияет, сервер пересчитывает сам.
  Подробности алгоритма и таблица порогов — `docs/service-types.md` §2/§6. Числа по
  умолчанию не выдуманы — сверены поиском с реальным тарифом «Грузовой» Яндекса
  (alfa-park-fleet.ru). Проверено на pg-mem: редактирование van сохраняет `size`
  нетронутым при правке остальных полей; невалидные/неполные пороги отклоняются;
  сохранённые пороги подхватываются и карточкой одобрения, и страницей специалиста;
  две разные комбинации см попадают в правильные тиры; нечисловой ввод отклоняется
  ошибкой, а не тихо мисклассифицируется. Автотеста в `tests/` не добавлено.

- **[x] 2026-09-16**, commits `ce2d4ec`, `fb697ca` — «Вывоз мусора» снова отдельная
  категория (была `body:flatbed` внутри `van`, см. поправку в `docs/service-types.md`
  §6), с новым обязательным полем «Объём кузова» (до 2/4/8/15+ м³, по образцу
  `max_tonnage` у эвакуатора — числа не выдуманы, сверены поиском реальной
  классификации самосвалов). Отдельно заводили и в тот же день убрали
  «Вывоз мусора (мешками, вручную)» — владелец указал, что это просто грузчики без
  техники, отдельная категория только путала выбор при регистрации; откат сделан
  guarded-миграцией (`DELETE ... WHERE slug='junk_manual' AND NOT EXISTS (SELECT 1
  FROM masters ...)`), не тронет деплой, где уже кто-то успел зарегистрироваться под
  ней. Проверено на pg-mem: регистрация самосвала без объёма отклоняется, с объёмом —
  проходит; `junk_manual` создаётся и корректно чистится, если не используется.

- **[x] 2026-09-16**, commits `c4016a7` — к уже существовавшему созданию категорий
  (`/admin/categories/new`, работало ещё с 09-06 — см. поправку в
  `docs/service-types.md` §6, раньше нигде явно не документировали) добавлено
  **удаление** (`POST /admin/categories/:slug/delete`). Заблокировано, если категорией
  уже пользуется хоть один специалист (`masters.category` или
  `master_services.service_type`) — иначе его профиль остался бы ссылаться на
  несуществующую категорию и сломал бы повторное одобрение/каталог/рассылку для него;
  подсказка при блокировке — снять «Категория активна» вместо удаления. Владелец
  просил это явно, чтобы «полностью управлять категориями из админки, не трогая код».
  Проверено на pg-mem: удаление занятой категории блокируется с понятным сообщением
  и числом специалистов, свежесозданная неиспользуемая категория удаляется чисто.

- **[x] 2026-09-16**, commits `9db9a2d`, `493ea2f` — модератор одобряет нового
  специалиста из Telegram лично, не через общий вход в `/admin`. Раньше кнопка
  в уведомлении вела в общую админку (пароль + SMS-2FA), и система не запоминала,
  кто именно одобрил — назначение менеджера работало только для реферальных
  регистраций. Теперь каждому модератору с включённым веб-доступом в `/manager`
  (см. `docs/manager-portal.md`) приходит личная одноразовая ссылка
  (`managerPortal.service.issueMagicLink`, Redis, TTL 72ч), которая логинит именно
  его и открывает карточку конкретного специалиста (`/manager/review/:id`) — клик по
  категории сразу и атомарно закрепляет заявку за этим модератором. Карточка
  переделана в два шага (`493ea2f`, по итогам того же дня: изначально все 5 категорий
  рисовались одновременно с уже открытыми полями — путано) — сначала кнопки категорий,
  потом только поля выбранной. Модератор без выданного веб-доступа по-прежнему
  получает старую ссылку на общую админку — деградация плавная. Подробности —
  `docs/manager-portal.md`, новый раздел «Личная ссылка одобрения из Telegram».
  Автотеста в `tests/` нет — проверено сессионными pg-mem-скриптами, стоит оформить
  в `tests/manager-portal.test.js`.

- **[x] 2026-09-16**, commits `0f3eb62`, `1675ef4`, `4ff0a21` — по итогам разбора
  владельцем реальной регистрации на `/join` (скриншоты процесса). Три находки в
  одном заходе:
  1. `0f3eb62` — карточка «как это работает» на экране успеха регистрации звала
     писать боту `@xtendergebot` для пополнения баланса — неверно (этот бот для
     привязки Telegram-уведомлений, не для оплаты; в кабинете уже есть самостоятельное
     пополнение переводом). Заодно СМС «заявка принята» была жёстко на русском вне
     зависимости от языка регистрации — теперь берётся `req.lang`.
  2. `1675ef4` — экран успеха убран целиком, после ввода кода из SMS сразу открывается
     кабинет (`window.location = data.link`), тот же приём уже применялся для
     композера заказа клиента. Удалены переводы/разметка, которые были нужны только
     этому экрану. Существующий свёрнутый блок-инструкция «как работать с заявками»
     в кабинете (был, просто мало кто открывал) теперь раскрыт по умолчанию, пока
     профиль на проверке — первые вопросы отвечены именно тогда, когда актуальны.
  3. `4ff0a21` — язык личного кабинета исполнителя (`/master/:token`) теперь берётся
     из сохранённого при регистрации `masters.language`, а не из куки `lang`
     браузера: ссылка на кабинет приходит по SMS и часто открывается в другом
     браузере/устройстве без нужной куки (или с чужой). Переключатель языка внутри
     кабинета обновляет именно это поле, не только куку — выбор следует за аккаунтом
     на любое устройство. Проверено на pg-mem (регистрация/повторная регистрация/
     мусорный язык нормализуется/переключатель обновляет сохранённое значение) и
     рендером обоих затронутых шаблонов.

- **[x] 2026-09-16**, commits `8b926b3`, `27fb6b7` — страница пополнения
  баланса (`src/views/topup.ejs`) переверстана по итогам диалога с владельцем
  (два раунда правок в одной сессии). Проблема: полные юр.реквизиты (IBAN/
  SWIFT/юр.адрес/ИНН и т.д.) были открыты на весь экран сразу, а чуть ниже те
  же IBAN/сумма/назначение ещё раз повторялись в блоке «через мобильный
  банк» — визуально путало; отдельно владелец подтвердил, что перевод через
  мобильный банк на счёт организации — основной способ оплаты, поход в
  отделение — вторичный, поэтому порядок и заметность блоков должны это
  отражать. Итог: карточка-саммари (бренд/сумма/№/статус, «Скачать PDF»,
  выбор языка PDF) сверху; **«Оплатить переводом»** (вкладки банков BoG/TBC/
  Credo с IBAN/суммой/назначением и копированием) — единственный открытый по
  умолчанию блок, сразу под саммари; юр.реквизиты и инструкция для отделения
  банка объединены в один свёрнутый блок **«Полный документ и оплата в
  отделении»** (внутри — «Распечатать» и новая кнопка «Копировать все
  реквизиты», копирует все поля одной строкой через `navigator.clipboard`).
  Текст в «После оплаты» смягчён — чек явно назван необязательным, т.к.
  админка уже умеет сверять поступления по банковской выписке (см. соседние
  коммиты в `admin.controller.js`/`bankStatement.service.js` — invoice search,
  auto-matching, ручное назначение неопознанных платежей); загрузка чека
  теперь описана как «ускоряет проверку», а не обязательный шаг. При печати
  (кнопка или Ctrl+P) свёрнутый блок с реквизитами принудительно
  раскрывается на время печати (`beforeprint`/`afterprint`) и сворачивается
  обратно — без этого на печать уходила бы пустая страница; это же событие
  использовалось, чтобы поймать регрессию первого раунда правок — форма
  загрузки чека и футер-примечания раньше скрывались в печати через общий
  `.payment-help{display:none}`, после переноса блоков перестали, пришлось
  добавить точечные print-правила взамен. Проверено на pg-mem dev-сервере
  через Playwright (не закоммичен, временный `_test-topup.js`/`_shot.js`):
  свёрнутое/развёрнутое состояние на 1280px и 390px, копирование отдельных
  полей и «копировать всё» (потребовалось явно выдать `clipboard-write` —
  headless Chromium без разрешения тихо роняет запись, не баг кода), эмуляция
  печати до и после фикса регрессии. Автотеста в `tests/` не добавлено — это
  вёрстка/копирайт без изменений бизнес-логики.

- **[x] 2026-09-15**, не закоммичено — «Грузчики»/«Эвакуатор»/«Автовышка»/
  «Вывоз мусора» (все встроенные категории, кроме van) теперь редактируются
  через `/admin/categories` — по просьбе владельца («хочу управлять системой
  из админки, не трогая код»). Раньше это блокировал флаг `is_builtin`
  целиком; заменил на прицельную проверку `hasLockedField()` — заблокирована
  только категория, у которой ЕСТЬ поле `input==='size'` (сейчас это только
  `van`: его «Размер» на самом деле проставляется старой колонкой
  `masters.vehicle_size` через отдельный `<select>` на странице исполнителя
  — см. `admin/master-detail.ejs`, а не этим движком — открывать его
  редактирование сейчас означало бы менять конфиг, ни на что не влияющий;
  объединение — отдельная задача, не делал).
  У вариантов enum-полей (список) машинное значение (`options: [value…]`)
  теперь отделено от подписи (`optionLabels: {value:{ru,ka,en}}`) — тем же
  приёмом, что уже был у названия самого поля. Иначе первое же сохранение
  «Эвакуатора»/«Автовышки» через старую плоскую форму спалило бы все
  грузинские/английские подписи вариантов («Паук (частичная погрузка)» и
  т.п.) на сырые ключи — их раньше просто негде было ввести повторно.
  `match`/`filter`/`optionIcons` не выведены в форму (движок подбора по
  атрибутам — Фаза 4b, всё ещё не написан) — при сохранении переносятся из
  прежней версии поля как есть, у новых полей — те же дефолты
  (`match:'ignore'`), что и раньше у пользовательских категорий.
  `src/views/admin/_category-field-option.ejs` — новый партиал (вложенный
  повторяемый список вариантов внутри повторяемого списка характеристик,
  добавление/удаление с перенумерацией по образцу уже существующего
  паттерна `_category-field.ejs`). Проверено на pg-mem: van остаётся
  заблокирован (попытка подсунуть свои поля через API — проигнорирована),
  у movers/tow/bucket_lift/junk работает добавление/удаление полей, смена
  unit/min/max, повторное сохранение формы БЕЗ изменений — истинный no-op
  (ничего не теряется), редактирование одного варianta enum с сохранением
  стабильного value у нетронутых и генерацией нового у добавленного,
  шаблоны рендерятся без JS-ошибок. Плюс живой прогон через dev-server:
  вход в админку, правка movers (unit + новое bool-поле) и tow (новый
  3-й вариант списка) через реальный браузер — сохранилось, при
  перезагрузке страницы видно всё как есть на скриншоте. Существующий
  `tests/dynamic-categories.test.js` пришлось поправить — рендер
  `category-edit.ejs` в нём теперь требует локаль `fieldsLocked` (её
  добавил контроллер), напрямую вызываемый `ejs.render()` в тесте эту
  локаль не знал. Полный набор `tests/*.test.js` прогнан — всё зелёное,
  кроме `moderated-signup.test.js`, который падает и на чистом `main` без
  моих изменений (не трогал, не по этой задаче).

- **[x] 2026-09-15**, commits `7188759`, `9f536bc`, `d8a4ca3` — два запроса
  владельца по итогам разбора `/admin/categories` (см. `docs/service-types.md`,
  Фаза 5, коммит `0880202` от 09-14, ранее не была отражена ни в этом файле,
  ни в памяти сессии — расхождение исправлено заодно).
  1) **Название характеристики** (`src/views/admin/_category-field.ejs`,
     `src/services/category.service.js`): грузинский/английский теперь
     необязательны — обязателен только русский (`parseFields`), модератор,
     работающий в одиночку, заполняет одно поле. Публичный бейдж на карточке
     каталога (`categoryService.view()`) при отсутствии перевода падает на
     русский текст, а не на сырой i18n-ключ (было бы видно как
     `svc_svc_xxxx_f_xxxx` грузинским/англ. посетителям). Название САМОЙ
     категории осталось обязательным на 3 языках — она крупно видна в
     каталоге/навигации, в отличие от мелкой подписи характеристики.
     Проверено на pg-mem (сохранение без ka/en не падает, вид с фолбэком
     корректный, отсутствие RU по-прежнему отклоняется).
  2) **Страница заявки** (`src/views/order.ejs`,
     `src/controllers/order.controller.js`, `src/config/i18n.js`): под
     статусом заявки — «Заявка создана N мин/ч назад» (для всех, кроме
     закрытых заявок). Дополнительно для исполнителя, открывшего заявку по
     своей ссылке (`?master=`, `masterAccount` в контроллере) — зелёная
     плашка «Вы отреагировали быстро» при ≤15 мин с момента создания, или
     красная «Вы открыли заявку спустя N ч — клиент, вероятно, уже нашёл
     исполнителя» при >60 мин. Пороги (15/60 мин) — мои дефолты по аналогии
     с tow/bucket_lift в своё время, владелец не подтверждал; период 15–60
     мин намеренно без плашки, чтобы не мельтешить. Считается от
     `orders.created_at`, а не от момента реальной рассылки этому конкретному
     мастеру (`balance_transactions.reason='lead_charge'.created_at`) — для
     заявок, ушедших этому мастеру в первой волне рассылки, разница обычно
     секунды, но при повторной/поздней ручной рассылке (админка) плашка может
     показать более раннее время, чем мастер реально получил лид; если это
     окажется вводящим в заблуждение — переключить на per-master notified_at.
     Проверено рендером `order.ejs` через `ejs.render()` напрямую (ru/ka/en,
     свежая/поздняя заявка, владелец/исполнитель, закрытая заявка — плашки не
     показываются) и живым сценарием через dev-server (создание заявки в
     браузере → OTP → открытие `/order/<token>?master=1`, реальный
     Tailwind-CSS, скриншот). `npm run build:css` пересобран (новые классы
     `bg-red-50`/`text-red-800` и т.п. — их не было в бандле раньше).
     Grammar/переводы ka не носителем — как и остальные строки `svc_*`,
     нужна вычитка.

- **[x] 2026-09-15**, commits `ecdc6c1`, `dba2769`, `610e7c9` — главная
  страница (`/`): героический блок над формой заявки переписан итеративно
  вместе с владельцем, три коммита — три шага одного диалога, не дубли.
  Финал: эйбрау/заголовок/лид-абзац/три буллета убраны совсем (`ecdc6c1`
  сначала переписал их текст, `dba2769` убрал блок целиком по просьбе
  владельца) — остался только `<h1 class="sr-only">` с тем же текстом
  (SEO/a11y, визуально не виден). На их месте — карточка «живых примеров»
  (`.home-examples--hero`, `src/views/index.ejs`): 8 сценариев по реальным
  категориям сервиса (вывоз мусора, переезд, грузчики, эвакуатор,
  автовышка — см. `src/config/serviceTypes.js`, категорий сантехника/
  плитка, которые изначально предлагал владелец, у платформы нет, заменены
  на реальные). Для каждого сценария — запрос клиента + 2 РАЗНЫХ мастера с
  разной ценой/сроком (нарочно: наглядно показывает саму суть сервиса —
  одна заявка → разные предложения). `610e7c9` добавил эффект печати:
  текст запроса и каждого ответа печатается посимвольно (JS
  `setInterval`+`paused`-флаг, не CSS-анимация — глобальное правило
  `prefers-reduced-motion` в `input.css` такое не гасит, поэтому явно
  проверяется `matchMedia('(prefers-reduced-motion: reduce)')` и текст при
  этом условии показывается сразу целиком). Данные вынесены в отдельный
  `src/config/home-examples-copy.js` (по образцу `topup-copy.js`/
  `provider-copy.js`, не пронумерованные ключи в `i18n.js`). На ka/ru/en;
  `i18n.js` менялся только в части, общей для всех языков (`home_title`
  для ka трогали только в `ecdc6c1`, ru/en остались прежними — владелец
  просил менять текст конкретно на грузинском). Проверено интерактивно
  через Playwright на `dev-server.js`: посимвольная печать по кадрам,
  переход между 8 примерами, пауза при наведении/фокусе (напрямую
  позиционированием курсора — штатный `page.hover()` спотыкается о
  постоянно меняющий размер элемент и даёт ложный результат), отсутствие
  ошибок в консоли, вёрстка на 1440px и 390px для ka/ru/en. Автотеста в
  `tests/` не добавлено — это статический маркетинговый копирайт главной,
  а не бизнес-логика; `tests/` целиком про БД/сервисы. Долг на будущее:
  `translate()` в `i18n.js` при отсутствии ключа молча возвращает сам ключ
  вместо ошибки — стоит когда-нибудь добавить тест на паритет ключей
  между ka/ru/en, иначе опечатка в одном языке проявится не крашем, а
  “сырым” текстом ключа на проде.
- **[x] 2026-09-15**, commit `d5783ef` — админ может удалять заявки (кнопка
  в списке `/admin/orders` и на странице заявки, с подтверждением — действие
  необратимо). `order.service.js` `deleteOrder()`: явно чистит
  `dispatch_runs`/`dispatch_deliveries` (без этого FK блокирует удаление —
  в схеме у них нет ON DELETE), отвязывает `crm_invites.order_id` (сам
  инвайт остаётся). `balance_transactions` и `sms_consent_logs` не трогает
  по замыслу схемы — списание с баланса и журнал согласий переживают
  удаление заявки. Событие `ORDER_DELETED` пишется в журнал согласий до
  удаления строки. Без миграции схемы (каскад сделан в транзакции
  приложения, а не ALTER на FK) — сознательно, не хотелось второй раз за
  сессию трогать `schema.sql` после утреннего краш-лупа. Тест:
  `tests/delete-order.test.js`.
- **[x] 2026-09-15**, commit `1169720` — префикс `+995` в полях телефона
  (`data-georgian-phone`) теперь нельзя стереть/заменить вводом или вставкой
  (раньше можно было выделить всё и вписать, например, `+7...` — форма это
  не мешала, отклонял только сервер после круга с OTP). `public/js/
  georgian-phone.js`: блокировка правок в зоне префикса + вставка из
  буфера обмена вставляет цифры после префикса, не затирая уже введённое.
  Проверено интерактивно через Playwright на dev-server.js — 7 сценариев
  (обычный ввод, попытка заменить префикс, backspace/delete на границе,
  вставка с уже включённым +995, вставка посреди набора). Не путать со
  следующей задачей (вход мастера) — это форма подачи заявки/регистрации,
  там отдельная регрессия нормализации на сервере.
- **[x] 2026-09-15**, commit `b104bfa` — таблица `card_payments` добавлена в
  `schema.sql` (структура на будущее для BOG, см. задачу выше — сама
  интеграция ещё не сделана, это не дубль).
- **[x] 2026-09-15**, commit `e26f423` (+ `400755b` документация) — исправлен
  краш-луп миграции на проде: view `business_orders`/`business_masters`
  (добавлены в `ae9a1c3`) блокировали `ALTER TABLE orders ALTER COLUMN token
  TYPE VARCHAR(20)` на каждом рестарте контейнера (`cannot alter type of a
  column used by a view or rule`). Фикс — `DROP VIEW IF EXISTS
  business_orders, business_masters CASCADE` перед прогоном `schema.sql` в
  `src/app.js`. Подробности и разбор причины: `current-state.md`.
- **[x] 2026-09-16**, commits `f0439fd`, `4042ea5` — по просьбе владельца
  («администратор должен видеть сформированные счета и мог подтвердить оплату и
  зачислить») `/admin/receipts` показывает ВСЕ созданные счета (`topup_requests`),
  а не только те, к которым прикреплён чек — раньше `receiptService.list()`
  строился от `topup_receipts`, и счёт без загруженного чека был администратору
  не виден вовсе. Кнопка «Подтвердить оплату и зачислить»
  (`receiptService.confirmPayment` → `masterService.adjustBalance`) — теперь
  единственное место, которое реально зачисляет по счёту; старое ручное
  зачисление через Telegram-бота `/topup` по телефону по-прежнему существует
  отдельно и не знает о счетах — см. задачу выше про риск двойного зачисления.
  Отдельно добавлена «Удалить счёт» (`receiptService.cancel`, мягкий статус
  `cancelled`, не `DELETE` — есть FK на чек и нужна история), т.к. исполнитель
  может создать несколько счетов и оплатить один. Проверено на pg-mem
  (`tests/topups.test.js` расширен) и живым прогоном через `dev-server.js`
  (счёт без чека подтверждён, баланс реально вырос; повторное подтверждение и
  отмена уже зачисленного счёта отклоняются с понятной ошибкой).
- **[x] 2026-09-16**, commits `54f9200`, `b0a41d7`, `ae53048`, `1e3145d` —
  сверка пополнений по банковской выписке (.xlsx, экспорт Bank of Georgia) —
  владелец вручную сверял платежи с банком и попросил автоматизировать поиск.
  Новый `src/services/bankStatement.service.js` (зависимость `exceljs`) парсит
  зачисления и ищет в назначении платежа номер вида `XT-XXXXXXXXXXXX` сразу в
  трёх колонках (`Nomination`, `Entry Comment`, `Additional Info` — на реальном
  файле владельца актуальный и устаревший номер оказались в РАЗНЫХ колонках
  одной строки, первая версия смотрела только в одну и находила не тот),
  сопоставляет с неоплаченными счетами и поднимает совпадения в начало списка
  с реальной суммой из выписки, а не запрошенной. Платежи без узнанного номера
  или с чужим номером — отдельный список для ручной привязки к выбранному
  счёту (`/admin/receipts/assign-statement-credit`). Дедупликация по номеру
  документа банка (`Doc N`, новая таблица `bank_statement_credits`) — без неё
  повторная или пересекающаяся по датам выписка могла бы привязать один и тот
  же перевод к двум разным счетам; кнопка «Пропустить» — для платежей, вообще
  не относящихся к балансу. Заодно исправлено: дата-ячейка Excel как объект
  `Date` рендерилась как `toString()` вместо `DD.MM.YYYY`. Тесты: новый
  `tests/bank-statement.test.js` (парсинг, сопоставление, дедуп, реальный
  сценарий с разными номерами в двух колонках). Живой прогон через
  `dev-server.js` на синтетических копиях реальных данных владельца.

Более ранняя история — в `current-state.md` (снимки состояния по датам) и
локальном `HANDOFF.md` (сессии по датам, если файл присутствует в среде).
